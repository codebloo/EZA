
<div class="play-step">
 
  <div class="gallery-image-title">{{ block.settings.title }}</div>
  <div class="gallery-image-text">{{ block.settings.text }}</div>
  <span class="gradient"></span>
</div>


{% schema %}
{
  "name": "Play Step",
  "settings": [ 

     
    {
      "type": "text",
      "id": "title",
      "label": "Time"},
          {
      "type": "textarea",
      "id": "text",
      "label": "Text"}
 
  ],
  "presets": [
    {
      "name": "Play Step",
    }
  ]
}
{% endschema %}
