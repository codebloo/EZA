<section class="eza-founder">
    <div class="section section--page-width">

        <div class="founder">
            <span class="founder-name">{{ section.settings.founder_name }}</span>
            <span class="founder-title">{{ section.settings.founder_title }}</span>
        </div>

        <div class="founder-content">
            <div class="content-eyebrow">{{ section.settings.eyebrow }}</div>
            <div class="content-title">{{ section.settings.title }}</div>
            <div class="content-text">{{ section.settings.text }}</div>
            <div class="content-attribution">{{ section.settings.attribution }}</div>
            <a href="{{ section.settings.link }}" class="button content-button">{{ section.settings.button }}</a>
        </div>

    </div>
</section>

<style>
.eza-founder{
    background:  rgba(255, 242, 0, 0.50);
    background: linear-gradient(180deg, rgba(0, 0, 0, 0.00) 20.19%, rgba(0, 0, 0, 0.80) 100%);}

.founder{}
.founder-name{}
.founder-title{}

.founder-content{
display:flex;
flex-direction:column;
gap:8px;}
.content-eyebrow{
color: #D3D3D3;
font-size: 16px;
font-weight: 700;
line-height: 1.1;}
.content-title{
color: var(--yellow);
font-size: 24px;
font-weight: 700;
line-height: 1.1;}

.content-text{
color:  #F1F1F1;
font-size: 16px;
font-weight: 400;
line-height: 21px; }

.content-attribution{}
.content-button{
    align-items: center;
display: flex;
gap: 10px;
height: 48px;
justify-content: center;
padding: 12px 24px;
width: 327px;
}
</style>