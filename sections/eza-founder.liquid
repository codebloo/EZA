<section class="eza-founder">
    <div class="section section--page-width">

        <div class="founder-name"></div>

        <div class="founder-content">
            <div class="eyebrow">{{ section.settings.eyebrow }}</div>
            <div class="title">{{ section.settings.title }}</div>
            <div class="text">{{ section.settings.text }}</div>
            <div class="attribution">{{ section.settings.attribution }}</div>
            <a href="{{ section.settings.link }}" class="button">{{ section.settings.button }}</a>
        </div>

    </div>
</section>

<style>
.eza-founder{
    background:  rgba(255, 242, 0, 0.50);
    background: linear-gradient(180deg, rgba(0, 0, 0, 0.00) 20.19%, rgba(0, 0, 0, 0.80) 100%);}

</style>